# InsrumentsBOT — Telegram‑хаб для SMM и постов в канал

> [!IMPORTANT]
> **Личный проект.** Собрал для **своей работы**: оформляю посты в канале с кейсами, веду публикации и рутину SMM без сторонних «постеров». **Исходники в публичный репозиторий не выкладываются** (токены, сессии MTProto, `bots.json`).

> [!NOTE]
> **Пользуюсь сам каждый день** — это не заказчикский продукт и не «демо ради GitHub». Витрина описывает реальный инструмент под мой канал и смежные задачи (в т.ч. для SMM‑специалистов: богатые посты, эмодзи, отложка).

## 💡 Кратко

**InsrumentsBOT** — связка из **центрального бота‑хаба** и **отдельных publisher‑ботов** (свой токен на каждого «постера»). Хаб: конвертация видео в **кружки** (включая MKV), привязка каналов, учёт ботов, MTProto‑сессии. Publisher: **конструктор постов** в канал — пересылка любого контента, URL‑кнопки с цветами, **премиум‑эмодзи**, «картинки из эмодзи», Markdown/HTML, **отложенная публикация**, альбомы.

## ✅ Что реализовано

### Хаб (`bot.py`)
- **Видео → кружок**: ffmpeg, квадрат 640×640, обрезка по длительности; вход — MP4, MKV и др. (удобно для 3D‑роликов).
- **Каналы**: добавление по пересылке, список «Мои каналы», синхронизация, часовой пояс по городу.
- **Боты пользователей**: хранение токенов publisher‑ботов в `bots.json`, автозапуск процессов `publisher_run`.
- **MTProto (Telethon)**: привязка user‑сессии для премиум‑эмодзи и проверок в канале.
- Защита от двойного запуска (lock‑файл), опционально **локальный Bot API** для файлов > 50 МБ.

### Publisher (`publisher.py`)
- **Создать пост**: переслать текст/фото/видео/стикеры — черновик под канал.
- **URL‑кнопки**: мастер «название → ссылка → цвет» (success / primary / danger), до 3 кнопок в ряд через `|`.
- **Премиум‑эмодзи** в тексте поста и в подписях кнопок; публикация с донастройкой entities после отложки.
- **Картинка из эмодзи**: нарезка кадра/анимации на сетку, custom emoji sticker set, вставка `<tg-emoji>` в пост.
- **Автоформатирование** Markdown / HTML, превью и «Обновить пост» в чате.
- **Отложка** с учётом таймзоны канала; фоновый edit премиум‑оформления после выхода поста в эфир.
- Публикация в канал: одиночные сообщения и **media group** (альбомы).

### Инфраструктура
- Состояние: `publisher_state.json`, `channels.json`, `hub_sessions.json` (шифрование сессий по `HUB_SESSION_SECRET`).
- Деплой на **Ubuntu**: systemd (`insruments-bot`, `insruments-publisher`), ffmpeg, venv, rsync‑скрипты (`DEPLOY_UBUNTU.md`).

## 🔌 Интеграции

- **Telegram Bot API** (aiogram 2), при необходимости **локальный telegram-bot-api**.
- **Telethon** (MTProto) для сессий и премиум‑сценариев.
- **ffmpeg** / ffprobe — кружки, эмодзи‑сетки, сжатие видео.

## 🧰 Технологии

| Категория | Стек |
|-----------|------|
| Язык | Python 3 |
| Telegram | aiogram 2, Telethon |
| Медиа | ffmpeg, ffprobe |
| Хранение | JSON (боты, каналы, черновики, отложки) |
| Инфраструктура | Ubuntu, systemd, bash, опционально local Bot API |

## 🎯 Зачем мне

- Оформление постов **«РАБОТЫ»** и других каналов: единый стиль, кнопки, эмодзи, отложка.
- Не зависеть от чужих controller‑ботов под чужие лимиты и UX.
- Быстро: кружок из MKV, пост из пересылки, премиум‑эмодзи без ручной возни в клиенте.

## 🖼️ Скриншоты

| # | Экран |
|---|--------|
| 1 | Хаб: главное меню |
| 2 | Конвертер: «Сделать кружок» |
| 3 | Publisher: онбординг / главное меню |
| 4 | Создание поста (черновик) |
| 5 | Мастер URL‑кнопок |
| 6 | «Картинка из эмодзи» |
| 7 | Отложка / публикация в канал |

Положи файлы `screen1.png` … `screen7.png` в [`docs/`](docs/) — см. [`docs/README.md`](docs/README.md).

## Исходный код

> [!NOTE]
> Код локально в `InsrumentsBOT/`. По запросу могу показать архитектуру (хаб ↔ publisher, state, MTProto), без передачи `.env` и токенов.

## Лицензия

> [!CAUTION]
> Описание и скрины — для портфолио. Проект личный; токены и сессии не публикуются.
